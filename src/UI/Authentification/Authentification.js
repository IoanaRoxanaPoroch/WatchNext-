import classes from "./Authentification.module.css";

const Authentification = ({ children }) => {
  return <div className={classes.authetification}>{children}</div>;
};
export default Authentification;
